export interface StudentResponse {
  id: string;
  userId: string;
  institutionId: string;
  campusId: string;
  courseId: string;
  departmentId: string;

  firstName: string;
  lastName: string;
  email: string;
  phoneNumber: string;

  rollNumber: string;
  batch: string;
  admissionDate: string;
}